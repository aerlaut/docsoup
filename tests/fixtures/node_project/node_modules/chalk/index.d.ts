// Stub type definitions for chalk (fixture)
export interface ChalkInstance {
  bold: ChalkInstance;
  red: ChalkInstance;
  (text: string): string;
}

export declare const chalk: ChalkInstance;
export default chalk;
